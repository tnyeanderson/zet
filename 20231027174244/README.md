# Update timestamps for unchanged commits using `git rebase`

To ensure that commit timestamps are updated when rebasing, use
`--ignore-date`:

```
git rebase --ignore-date
```

    #git #rebase #tips
